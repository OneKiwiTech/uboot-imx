if TARGET_IMX8MM_MAAXBOARD

config SYS_BOARD
	default "imx8mm_maaxboard"

config SYS_VENDOR
	default "freescale"

config SYS_CONFIG_NAME
	default "imx8mm_maaxboard"

source "board/freescale/common/Kconfig"

endif
